# use git repository at local locationa
spring.cloud.config.server.git.uri=file:///${user.home}/config-repo

#Pointing to the local directory
#spring.cloud.config.server.git.uri=${user.home}/config-repo

server.port=8888
